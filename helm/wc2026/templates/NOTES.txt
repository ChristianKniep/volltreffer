{{ .Chart.Name }} {{ .Chart.AppVersion }} has been deployed as release "{{ .Release.Name }}".

Access the app:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if eq .Values.service.type "LoadBalancer" }}
  Wait for the LoadBalancer IP:
    kubectl get svc -w {{ include "wc2026.fullname" . }} -n {{ .Release.Namespace }}
{{- else }}
  Port-forward to reach it locally:
    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "wc2026.fullname" . }} 8000:{{ .Values.service.port }}
  then open http://localhost:8000
{{- end }}

Admin login:
  user:     {{ .Values.config.adminUser }}
  password: (the value of secrets.adminPassword — change it after first login)

{{- if not .Values.secrets.appSecretKey }}

NOTE: You did not set secrets.appSecretKey, so the chart generated one and will
preserve it across upgrades. If you ever delete the Secret, stored betting
credentials become unreadable. Set a stable value to manage it yourself.
{{- end }}

{{- if gt (int .Values.replicaCount) 1 }}

WARNING: replicaCount is {{ .Values.replicaCount }}. This app uses SQLite on a
ReadWriteOnce volume and supports only ONE replica. Set replicaCount: 1.
{{- end }}
